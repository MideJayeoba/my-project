# VoiceMedAI backend — FastAPI production dependencies
PyJWT>=2.8.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic[email]>=2.0.0
python-dotenv>=1.0.0
numpy>=1.24.0
httpx>=0.27.0

# Triage classification models (models/triage_models_small.joblib)
scikit-learn>=1.4.0
joblib>=1.3.0

# Decode WebM on server (browser sends non-WAV)
av>=12.0.0

# Text-to-speech for Nigerian accent (en-NG)
edge-tts>=7.2.8

# PostgreSQL DB driver for Supabase
psycopg2-binary>=2.9.0
