# Data split manifests

Place CSV manifests in this directory for reproducible experiments:
- `train.csv`
- `val.csv`
- `test.csv`

Each row should include `audio_path`, `transcript`, `language_tags`, and `source`.
